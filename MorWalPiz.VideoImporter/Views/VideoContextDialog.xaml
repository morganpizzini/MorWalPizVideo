<Window x:Class="MorWalPiz.VideoImporter.Views.VideoContextDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MorWalPiz.VideoImporter.Views"
        mc:Ignorable="d"
        Title="Contesto Video"
        Height="550"
        Width="650"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{StaticResource BackgroundBrush}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Selected Files Card -->
        <Border Grid.Row="0" Style="{StaticResource Card}" Margin="0,0,0,20">
            <StackPanel>
                <TextBlock Text="📁 File Selezionati"
                           FontSize="16"
                           FontWeight="SemiBold"
                           Margin="0,0,0,10"/>
                
                <ListBox x:Name="SelectedFilesListBox"
                         ItemsSource="{Binding SelectedFiles}"
                         Height="180"
                         Background="White"
                         BorderBrush="{StaticResource BorderBrush}"
                         BorderThickness="1">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Margin="4">
                                <TextBlock Text="{Binding DisplayName}"
                                           FontWeight="SemiBold"
                                           TextTrimming="CharacterEllipsis"/>
                                <TextBox Text="{Binding Context, UpdateSourceTrigger=PropertyChanged}"
                                         Margin="0,4,0,0"
                                         ToolTip="Contesto specifico per questo video"/>
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </StackPanel>
        </Border>

        <!-- Video Context Card -->
        <Border Grid.Row="1" Style="{StaticResource Card}" Margin="0,0,0,15">
            <StackPanel>
                <TextBlock Text="📝 Contesto Video"
                           FontSize="16"
                           FontWeight="SemiBold"
                           Margin="0,0,0,5"/>
                
                <TextBlock Text="Inserisci il contesto specifico per ogni video. I video con lo stesso contesto verranno elaborati insieme."
                           Foreground="{StaticResource TextSecondaryBrush}"
                           Margin="0,0,0,10"
                           TextWrapping="Wrap"/>
            </StackPanel>
        </Border>

        <!-- Action Area -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Loading Indicator -->
            <StackPanel Grid.Column="0"
                        Orientation="Horizontal"
                        VerticalAlignment="Center"
                        Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}">
                <TextBlock Text="⏳ Elaborazione in corso..."
                           VerticalAlignment="Center"
                           FontWeight="SemiBold"
                           Margin="0,0,10,0"/>
                <ProgressBar IsIndeterminate="True"
                             Width="100"
                             Height="4"
                             VerticalAlignment="Center"/>
            </StackPanel>

            <!-- Buttons -->
            <StackPanel Grid.Column="1"
                        Style="{StaticResource DialogFooter}">
                <Button Content="Annulla"
                        Style="{StaticResource SecondaryButton}"
                        Width="90"
                        Click="CancelButton_Click"/>

                <Button x:Name="ConfirmButton"
                        Content="✅ Invia"
                        Style="{StaticResource PrimaryButton}"
                        Width="90"
                        Margin="10,0,0,0"
                        Click="ConfirmButton_Click"
                        IsDefault="True"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>