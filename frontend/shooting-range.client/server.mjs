import express from 'express'; import { readFileSync, writeFileSync } from 'node:fs'; import { resolve } from 'node:path'; const { render } = await import('./dist-ssr/entry-server.js'); const app = express(); const dist = resolve('dist'); writeFileSync(resolve(dist, 'env-config.js'), `window.ENV={API_BASE_URL:'${process.env.API_BASE_URL ?? ''}'};`); const template = readFileSync(resolve(dist, 'index.html'), 'utf8'); app.use(express.static(dist, { index: false })); app.get('*', async (request, response) => { const host = request.headers.host ?? 'localhost'; const result = await render(new Request(`http://${host}${request.originalUrl}`)); response.status(200).send(template.replace('<div id="root"></div>', `<div id="root">${result.html}</div>`)); }); app.listen(process.env.PORT ?? 5177);