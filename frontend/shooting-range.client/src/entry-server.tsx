import { renderToString } from 'react-dom/server'; import { createStaticHandler, createStaticRouter, StaticRouterProvider } from 'react-router'; import { routes } from './routes'; import './styles.css'; export async function render(request: Request) { const handler = createStaticHandler(routes); const context = await handler.query(request); if (context instanceof Response) throw context; const router = createStaticRouter(handler.dataRoutes, context); return { html: renderToString(<StaticRouterProvider router={router} context={context} />), head: '' }; }
