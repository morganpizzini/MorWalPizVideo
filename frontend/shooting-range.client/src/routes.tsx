import { useEffect, useState } from 'react';
import { type RouteObject } from 'react-router';
import { adminUsers, messages, openMessage } from './api';
import { App } from './App';
function Messages() { const [items, setItems] = useState<unknown[]>([]); const [text, setText] = useState(''); const load = () => { void messages().then(setItems).catch(() => setItems([])); }; useEffect(load, []); return <section className="panel"><span className="eyebrow">CONTATTI</span><h2>Messaggi</h2><p className="muted">Apri una domanda per il team del campo. Gli admin vedono tutte le conversazioni.</p><form onSubmit={async e => { e.preventDefault(); await openMessage({ subject: 'Richiesta dal portale', text }); setText(''); load(); }}><textarea required minLength={3} maxLength={2000} value={text} onChange={e => setText(e.target.value)} placeholder="Come possiamo aiutarti?" /><button>Invia messaggio</button></form><div className="message-list">{items.map((item, index) => <article className="message" key={index}>{JSON.stringify(item)}</article>)}</div></section>; }
function Admin() { const [users, setUsers] = useState<unknown[]>([]); useEffect(() => { adminUsers().then(setUsers).catch(() => setUsers([])); }, []); return <section className="panel"><span className="eyebrow">CONTROL ROOM</span><h2>Approvazioni</h2><p className="muted">Gestisci gli account in attesa e supervisiona le richieste.</p><div className="message-list">{users.map((item, index) => <article className="message" key={index}>{JSON.stringify(item)}</article>)}</div></section>; }
export const routes: RouteObject[] = [{ path: '/', Component: App }, { path: '/messages', Component: Messages }, { path: '/admin', Component: Admin }];
