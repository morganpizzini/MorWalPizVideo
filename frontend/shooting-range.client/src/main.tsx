import { StrictMode } from 'react'; import { createRoot, hydrateRoot } from 'react-dom/client'; import { createBrowserRouter } from 'react-router'; import { RouterProvider } from 'react-router/dom'; import { routes } from './routes'; import './styles.css'; const app = <StrictMode><RouterProvider router={createBrowserRouter(routes)} /></StrictMode>; const root = document.getElementById('root')!; root.innerHTML.trim() ? hydrateRoot(root, app) : createRoot(root).render(app);
